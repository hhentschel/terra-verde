<textarea id="lineitem-note-{{ item.id }}"
  class="input"
  type="text"
  name="lineItems[{{ item.id }}][note]"
  value="{{ item.note }}"
  placeholder="Text für Grusskarte"
  rows="2">
  {{ item.note }}
</textarea>
